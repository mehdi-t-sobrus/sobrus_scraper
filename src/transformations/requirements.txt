# =============================================================================
# src/transformations/requirements.txt
# Silver layer — dbt-duckdb + HTML parsing dependencies
# Kept separate from src/backend/requirements.txt to avoid DuckDB
# version conflicts with Django's dependency tree.
# =============================================================================

# -- dbt core + DuckDB adapter
dbt-core>=1.8,<2.0
dbt-duckdb>=1.9,<2.0        # pins a compatible DuckDB version automatically
duckdb>=1.0,<2.0

# -- HTML parsing (Bronze → Silver extraction)
selectolax>=0.3,<0.4        # fast HTML parser — never BeautifulSoup4 (CLAUDE.md §2)

# -- Data processing
pandas>=2.2,<3.0
pyarrow>=18.0,<25.0        # Parquet read/write backend — 18+ required for Python 3.13 wheels

# -- R2 / S3 object storage
boto3>=1.34,<2.0
botocore>=1.34,<2.0

# -- Utilities
python-dotenv>=1.0,<2.0
